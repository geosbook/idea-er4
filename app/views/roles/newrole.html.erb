<h1>New Role for Mission <%= @mission.name%></h1>

<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
    </div>

    <%= f.label :parent_mission %> 
    <%= @missionparent.name %>
  
  <div class="field">
    <%= f.hidden_field :parent_id, {value: @mission.id} %> 
  </div>
<br />
<br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', missions_path %>